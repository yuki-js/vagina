name: vagina_audio
description: Audio recording and playback module for VAGINA app
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.10.1
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  record: ^5.2.0
  just_audio: ^0.9.46
  permission_handler: ^11.4.0
  vagina_core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
