// Intentionally left blank.
//
// `hasApiKeyProvider` was removed to simplify configuration access.
// Call sites should read `configRepositoryProvider` and call `hasApiKey()` directly.
//
// Kept as a file placeholder to avoid churn in the refactor sequence.
