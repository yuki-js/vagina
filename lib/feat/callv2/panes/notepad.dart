import 'package:flutter/material.dart';

class NotepadPane extends StatelessWidget {
  const NotepadPane({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.expand();
  }
}
