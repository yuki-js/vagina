diff --git a/android/src/main/java/xyz/canardoux/TauEngine/FlautoPlayerEngine.java b/android/src/main/java/xyz/canardoux/TauEngine/FlautoPlayerEngine.java
index 3d9467b..XXXXXXX 100644
--- a/android/src/main/java/xyz/canardoux/TauEngine/FlautoPlayerEngine.java
+++ b/android/src/main/java/xyz/canardoux/TauEngine/FlautoPlayerEngine.java
@@ -36,6 +36,9 @@ import java.util.ArrayList;
 class FlautoPlayerEngine extends FlautoPlayerEngineInterface {
 	AudioTrack audioTrack = null;
 	int sessionId = 0;
+	// Thread synchronization for AudioTrack access (fixes race condition in feed())
+	private final Object audioLock = new Object();
+	private volatile boolean isPlaying = false;
 	long mPauseTime = 0;
 	long mStartPauseTime = -1;
 	long systemTime = 0;
@@ -108,34 +111,6 @@ class FlautoPlayerEngine extends FlautoPlayerEngineInterface {
 	*/
 
-	class FeedThread extends Thread {
-		byte[] mData = null;
-
-		/* ctor */ FeedThread(byte[] data) {
-			mData = data;
-		}
-
-		public void run() {
-			int ln = 0; // The number of bytes accepted (and perhaps played) by the device
-			if (mCodec == Flauto.t_CODEC.pcmFloat32)
-			{
-				ByteBuffer buf = ByteBuffer.wrap(mData);
-				buf.order(ByteOrder.nativeOrder());
-				FloatBuffer fbuf = buf.asFloatBuffer();
-				float[] ff = new float[mData.length/4];
-				fbuf.get(ff);
-				ln = audioTrack.write(ff, 0, mData.length/4, AudioTrack.WRITE_BLOCKING);
-				ln = 4 * ln;
-			} else
-			{
-				ln = audioTrack.write(mData, 0, mData.length, AudioTrack.WRITE_BLOCKING);
-			}
-			mSession.needSomeFood(1);
-		}
-	}
-
 	class FeedInt16Thread extends Thread {
 		ArrayList<byte[]> mData = null;
 
@@ -247,6 +222,7 @@ class FlautoPlayerEngine extends FlautoPlayerEngineInterface {
 
 	void _play() {
 		audioTrack.play();
+		isPlaying = true;
 
 	}
 
@@ -254,9 +230,14 @@ class FlautoPlayerEngine extends FlautoPlayerEngineInterface {
 	void _stop() {
 		if (audioTrack != null) {
-			audioTrack.stop();
-			audioTrack.release();
-			audioTrack = null;
+			isPlaying = false;
+			synchronized(audioLock) {
+				if (audioTrack != null) {
+					audioTrack.stop();
+					audioTrack.release();
+					audioTrack = null;
+				}
+			}
 		}
 		// blockThread = null;
 	}
@@ -298,9 +279,23 @@ class FlautoPlayerEngine extends FlautoPlayerEngineInterface {
 	}
 
 	int feed(byte[] data) throws Exception {
-		FeedThread t = new FeedThread(data);
-		t.start();
-		return 0;
+		// Synchronized feed to prevent race condition with _stop()
+		// See: https://github.com/Canardoux/flutter_sound/issues/1123
+		if (!isPlaying) return 0;
+		
+		synchronized(audioLock) {
+			if (audioTrack != null && audioTrack.getPlayState() == AudioTrack.PLAYSTATE_PLAYING) {
+				if (mCodec == Flauto.t_CODEC.pcmFloat32) {
+					ByteBuffer buf = ByteBuffer.wrap(data);
+					buf.order(ByteOrder.nativeOrder());
+					FloatBuffer fbuf = buf.asFloatBuffer();
+					float[] ff = new float[data.length/4];
+					fbuf.get(ff);
+					audioTrack.write(ff, 0, data.length/4, AudioTrack.WRITE_BLOCKING);
+				} else {
+					audioTrack.write(data, 0, data.length, AudioTrack.WRITE_BLOCKING);
+				}
+			}
+		}
+		mSession.needSomeFood(1);
+		return 0;
 	}
 }
